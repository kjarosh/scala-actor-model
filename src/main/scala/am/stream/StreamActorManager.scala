package am.stream

import am.{AbstractActorManager, Actor}

class StreamActorManager extends AbstractActorManager {
  override def register(actor: Actor): Unit = {

  }

  override def shutdown(): Unit = {

  }
}
